#include<stdio.h>
int main()
{
    int a,s;
    scanf("%d%d",&a,&s);
    if(a==s)
    {
        printf("It is the final match\n");
    }
    else{printf("It is not the final match\n");}

    return 0;
}
